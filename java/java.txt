1、Java 中如果要在一个方法声明中说明该方法执行时可能会抛出异常，则需要使用 throw 关键字。
2、Java 语言中异常的处理包括：声明异常、抛出异常、捕获异常、处理异常四个环节。
3、在 java.io 中，字符输出流类都是 Writer 类的子类。
4、Swing 中的内部框架类是 JFrame。
5、Java 中的异常对象是 Error 类或 Exception 类的对象，这两类对象中 Error 类的对象不会被 java 应用程序捕获和抛出。
6、在 Java 语言中，所有运行时异常的直接父类是 RuntimeException 类。
7、Swing 是由纯 Java 实现的轻量级构件，没有本地代码，不依赖’操作‘系统的支持。这是它与 AWT 构件的最大区别。
8、Swing 中的按钮类是 JButton 类。
9、类是变量和’方法‘的集合。
10、表达式 (10 * 49.3) 的数据类型是 double 的。
11、用户类型直接调用构造方法，只能通过 new 关键字自动调用。
12、构造方法是类中的一种特殊方法，是为对象初始化操作编写的方法。
13、Java 语言中，使用关键字 super 对当前对象的父类对象进行引用。
14、在 Java 语言中，用 abstract 修饰符定义的类为抽象类。
15、switch 语句中，在每个 case 字句后进行跳转的语句是：break。
1、为使 Java 程序独立于平台，Java 虚拟机把字节码和各个操作系统组件分开。
2、Java 语言与 C++ 语言相比，最突出的特点是’跨平台‘。
3、Java 源程序结构中前三种语句的次序：import、public、class。
4、在 JDK 目录中，Java 程序运行环境的根目录是 bin 目录。
5、== 属于关系运算符。
6、Java 语言中，定义布尔变量并赋值：boolean canceled = false;
7、final 关键字可用于表示常量
8、Java 中的基本数据类型 int 在不同的操作系统平台子长都是 32 位。
9、short 变量范围是 -322768 ~ +32767
10、break 属于跳转语句
11、switch 语句中表达式(expression)的值不允许为 boolean 类型。
12、for (;;) {} 可作为无限循环语句。
13、continue 语句跳转语句。
14、Java 语言中，new 作为内存分配运算符。
15、接口中除了抽象方法外，还可以含有常量。
16、用 str,length() 表示字符串的长度。
17、StringBuffer 类字符串对象的长度是’可变‘的。
18、子类继承了父类的方法和状态，在子类可以进行的操作是’添加方法‘。
19、STring、StringBuffer 都是 final 类，都不能被继承。
20、继承是面向对象编程的一个重要特征，它可降低程序的复杂性并可使’代码重用‘。
21、在 Java 语言中，能提供图形界面的包是’javax.swing'。
22、常见事件适配器：MoustAdpter、KeyAdpter、ComponentAdper
33、自定义异常类的父类可以是 Exception
34、Reader 类所处理的是’字符流‘
35、用于设置组建大小的是'setSize()'
36、RandomAccessFile 是 java.io 包中的一个兼有输入输出功能的类，由于它是随机访问，所以文件读写一个记录的位置是‘任意’
